name: AVD test

on: push

jobs:
    avd:
        runs-on: ubuntu-20.04
        container: reactnativecommunity/react-native-android:1.0.5
        steps:
            - run: echo no | avdmanager create avd -n test -k "system-images;android-21;google_apis;armeabi-v7a"